package model;

public class calculator {
    public static String decToQuat(int decimal){
        return ""; // I think we store quat numbers as strings so they aren't accidentally misinterpreted as ints (which use decimal)
    }

    public static int quatToDec(String quaternary){
        return 0;
    }


}
